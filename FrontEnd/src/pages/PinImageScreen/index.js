import React from 'react';
import PropTypes from 'prop-types';

PinImageScreen.propTypes = {
    
};

function PinImageScreen(props) {
    return (
        <div>
            <h1>PinImageScreen</h1>
        </div>
    );
}

export default PinImageScreen;